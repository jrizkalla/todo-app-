<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>UIBackgroundModes</key>
	<array>
		<string>remote-notification</string>
		<string>location</string>
	</array>
	<!--
	  How the Control Center button reaches the app. The scheme is declared in
	  one place in code — see `AppURL` — and this entry is what registers it
	  with the system; the two have to say the same thing.
	-->
	<key>CFBundleURLTypes</key>
	<array>
		<dict>
			<key>CFBundleURLName</key>
			<string>com.johnrizkalla.app.TODO</string>
			<key>CFBundleURLSchemes</key>
			<array>
				<string>todoapp</string>
			</array>
		</dict>
	</array>
	<!--
	  The drag payload used to move a to-do between lists and onto the
	  calendar. Declared as its own type, conforming to `public.data`, so a
	  drag from the Inbox is offered only to the app's own drop targets — a
	  plain-text payload would be accepted by every text field on screen, and
	  any text dragged in from another app would look like a to-do.
	-->
	<key>UTExportedTypeDeclarations</key>
	<array>
		<dict>
			<key>UTTypeIdentifier</key>
			<string>com.johnrizkalla.app.TODO.todo-item</string>
			<key>UTTypeDescription</key>
			<string>To-Do</string>
			<key>UTTypeConformsTo</key>
			<array>
				<string>public.data</string>
			</array>
			<key>UTTypeTagSpecification</key>
			<dict/>
		</dict>
	</array>
</dict>
</plist>
